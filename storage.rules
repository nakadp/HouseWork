rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /families/{familyId}/{allPaths=**} {
      // 允许登录用户上传和读取照片
      allow read, write: if request.auth != null;
    }
  }
}
